/**
 * Whether the tutoring view (/maths, docs/koncept.md §3.3) is open. While it is
 * not, the navigation shows it greyed out as "soon", /maths shows a short
 * "in preparation" notice instead of the offer (noindex, out of the sitemap)
 * and the terminal does not link to it. Flip to true to bring it back.
 */
export const tutoringOpen = false;
